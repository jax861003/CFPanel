{
  "name": "deploy-panel-v2",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "deploy": "wrangler pages deploy public --project-name deploy-panel-v2",
    "build:upload": "node scripts/build-upload.mjs",
    "pack:upload": "npm run build:upload && node scripts/zip-upload.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.53.0"
  },
  "engines": {
    "node": ">=20"
  }
}
